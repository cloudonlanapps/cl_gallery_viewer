name: cl_gallery_viewer_upload_example
description: Demo app for the MediaUploader module — uses an in-memory fake backend (no SDK / network).
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  cl_gallery_viewer:
    path: ../
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
