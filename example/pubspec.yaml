name: cl_gallery_viewer_example
description: Example app to test video player implementations
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  cl_gallery_viewer:
    path: ../
  file_picker: 8.3.7
  flutter:
    sdk: flutter
  media_kit: 1.2.6

dev_dependencies:
  flutter_lints: 6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
